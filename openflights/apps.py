from django.apps import AppConfig


class OpenFlightsConfig(AppConfig):
    """Django app configuration for OpenFlights."""

    name = "openflights"
    verbose_name = "OpenFlights"
    default_auto_field = "django.db.models.BigAutoField"
